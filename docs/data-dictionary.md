# Data dictionary

Generated by `scripts/06_docs.py` from `db/schema.sql`, the provenance registry and the built database. Do not edit by hand.

**Legend** — 🟢 REAL: copied from an official or cited source · 🟡 DERIVED: computed from real data by a documented rule · 🔵 CURATED: Bricks knowledge layer · ⚪ MOCK: simulated, to be replaced by the client system named under *Production source*.

| Origin | Tables | Rows |
|---|---|---|
| 🟢 REAL (sourced) | 9 | 6,564 |
| 🟡 DERIVED from real data | 5 | 645 |
| 🔵 CURATED knowledge | 12 | 825 |
| ⚪ MOCK (simulated) | 25 | 4,333 |

## 0. Meta

### `data_sources` — 8 rows

🔵 CURATED knowledge — Where every fact came from.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `code` | TEXT | required; unique | e.g. 'srichand_store_api' |
| `name` | TEXT | required |  |
| `url` | TEXT |  |  |
| `source_type` | TEXT | required; one of: official,retailer,third_party_db,regulator,news,curated,internal_mock |  |
| `accessed_at` | TIMESTAMP |  |  |
| `notes` | TEXT |  |  |

### `data_provenance` — 51 rows

🔵 CURATED knowledge — This registry.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `table_name` | TEXT | PK |  |
| `origin` | TEXT | required; one of: real_sourced,derived,curated_knowledge,mock |  |
| `description` | TEXT | required |  |
| `production_source` | TEXT |  | what system replaces this table in production (ERP, OMS, CRM…) |

## 1. Catalogue (real, sourced)

### `brands` — 3 rows

🟢 REAL (sourced) — Brands sold on srichand.com.  
*Production source:* Product master data (PIM/ERP)

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT | required |  |
| `positioning` | TEXT |  |  |
| `target_audience` | TEXT |  |  |
| `price_tier` | TEXT | one of: value,mass,masstige |  |
| `site_url` | TEXT |  |  |
| `source_id` | INTEGER | → data_sources.id |  |

### `product_lines` — 44 rows

🟡 DERIVED from real data — Collections inferred from official product names; positioning text is a curated reading of official copy.  
*Production source:* PIM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `brand_id` | INTEGER | → brands.id; required |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT |  |  |
| `domain` | TEXT | required; one of: skincare,sunscreen,powder,base_makeup,colour_makeup,lip,baby,hair,tools,mixed |  |
| `positioning_en` | TEXT |  | one-line purpose of the line |
| `hero_ingredients` | JSON |  | as named in official copy |
| `source_id` | INTEGER | → data_sources.id |  |

### `categories` — 48 rows

🟡 DERIVED from real data — Advisor-friendly category tree mapped from the site's own categories.  
*Production source:* PIM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `parent_id` | INTEGER | → categories.id |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT |  |  |
| `sort_order` | INTEGER | required |  |

### `products` — 190 rows

🟢 REAL (sourced) — Canonical products de-duplicated from 345 site listings. Official Thai copy verbatim; English name/summary, finish, coverage, oil-control hours are DERIVED from that copy.  
*Production source:* PIM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `brand_id` | INTEGER | → brands.id; required |  |
| `line_id` | INTEGER | → product_lines.id |  |
| `category_id` | INTEGER | → categories.id |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT | required |  |
| `product_type` | TEXT | required; one of: single,set,gift_with_purchase,accessory,gift_card |  |
| `lifecycle_status` | TEXT | required; one of: active,legacy,clearance_only |  |
| `tagline_th` | TEXT |  | official short description |
| `description_th` | TEXT |  | official long description (verbatim, HTML stripped) |
| `how_to_use` | TEXT |  | official directions from the product page's how-to-use tab (verbatim) |
| `summary_en` | TEXT |  | DERIVED: English summary of the official Thai copy |
| `texture` | TEXT |  |  |
| `finish` | TEXT | one of: matte,semi_matte,natural,satin,glowy,sheer,tone_up |  |
| `coverage` | TEXT | one of: none,sheer,light,medium,medium_to_full,full |  |
| `spf_value` | TEXT |  | '50+', '35' … as printed |
| `pa_rating` | TEXT |  | 'PA++++' |
| `oil_control_hours_claimed` | INTEGER |  | official claim, e.g. 'คุมมัน 14 ชั่วโมง' -> 14 |
| `is_water_resistant_claimed` | BOOLEAN |  | official copy says กันน้ำ / waterproof |
| `free_from` | JSON |  | official "free from" claims e.g. ["paraben","fragrance"] |
| `is_fragrance_free` | BOOLEAN |  | NULL = not stated |
| `is_alcohol_free` | BOOLEAN |  |  |
| `tested_claims_th` | TEXT |  | official test footnotes (e.g. irritation test n=32, Dermaproof Asia) |
| `official_url` | TEXT |  |  |
| `image_url` | TEXT |  |  |
| `review_count_site` | INTEGER |  |  |
| `average_rating_site` | NUMERIC(3,2) |  |  |
| `source_id` | INTEGER | → data_sources.id; required |  |
| `confidence` | TEXT | required; one of: official,retailer,third_party,unverified |  |
| `verified_at` | TIMESTAMP | required |  |
| `curation_notes` | TEXT |  |  |

### `product_variants` — 598 rows

🟢 REAL (sourced) — Every purchasable SKU with official SKU code, list price, sale price and web stock flag at snapshot time. Shade undertone/depth are DERIVED from official shade names.  
*Production source:* PIM + pricing engine

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `product_id` | INTEGER | → products.id; required |  |
| `sku` | TEXT | required; unique | official SKU from the site |
| `variant_label` | TEXT | required | '30 ml', '9 g · 120 Beige', 'Box of 6 × 10 ml sachets' |
| `pack_format` | TEXT | required; one of: full_size,mini,sachet,sachet_box,multipack,set,gift,other |  |
| `units_per_pack` | INTEGER | required |  |
| `size_value` | NUMERIC(8,2) |  |  |
| `size_unit` | TEXT | one of: ml,g |  |
| `shade_code` | TEXT |  |  |
| `shade_name` | TEXT |  |  |
| `shade_undertone` | TEXT | one of: cool_pink,neutral,warm_yellow,warm_golden,peach,corrector | DERIVED from official shade name |
| `shade_depth_rank` | INTEGER |  | DERIVED: 1 = lightest within the product (from official shade numbering) |
| `shade_guidance_th` | TEXT |  | official 'suits which skin tone' line, verbatim, where published |
| `list_price` | NUMERIC(10,2) | required | official regular price (THB) |
| `sale_price` | NUMERIC(10,2) |  | official current price if discounted, else NULL |
| `currency` | TEXT | required |  |
| `web_in_stock` | BOOLEAN | required | srichand.com availability flag at snapshot time (real) |
| `is_listed_on_site` | BOOLEAN | required |  |
| `site_listing_id` | INTEGER |  |  |
| `site_variation_id` | INTEGER |  |  |
| `barcode` | TEXT |  | not published; NULL until client master data is loaded |
| `price_checked_at` | TIMESTAMP | required |  |

### `bundle_components` — 76 rows

🟡 DERIVED from real data — Set contents matched from official set names/descriptions by text. Verify against client BOM.  
*Production source:* PIM / BOM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `bundle_variant_id` | INTEGER | → product_variants.id; required |  |
| `component_variant_id` | INTEGER | → product_variants.id | NULL when the component is not sold separately |
| `component_product_id` | INTEGER | → products.id |  |
| `component_label` | TEXT | required |  |
| `quantity` | INTEGER | required |  |
| `is_free_gift` | BOOLEAN | required |  |

### `site_listings` — 345 rows

🟢 REAL (sourced) — Raw snapshot of all 345 public listings and how each maps to a canonical product.  
*Production source:* E-commerce platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `site_listing_id` | INTEGER | PK |  |
| `product_id` | INTEGER | → products.id |  |
| `listing_name` | TEXT | required |  |
| `listing_type` | TEXT | required; one of: regular,sachet_box,multipack,set,gift,clearance,duplicate |  |
| `permalink` | TEXT | required |  |
| `regular_price` | NUMERIC(10,2) |  |  |
| `current_price` | NUMERIC(10,2) |  |  |
| `on_sale` | BOOLEAN | required |  |
| `in_stock` | BOOLEAN | required |  |
| `variation_count` | INTEGER | required |  |
| `site_categories` | JSON |  |  |
| `snapshot_at` | TIMESTAMP | required |  |

### `product_images` — 878 rows

🟢 REAL (sourced) — Official image URLs (hot-linked, not copied).  
*Production source:* DAM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `product_id` | INTEGER | → products.id; required |  |
| `url` | TEXT | required |  |
| `alt_text` | TEXT |  |  |
| `sort_order` | INTEGER | required |  |

### `product_claims` — 213 rows

🟢 REAL (sourced) — Official claim sentences, verbatim Thai.  
*Production source:* Regulatory-approved claims library

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `product_id` | INTEGER | → products.id; required |  |
| `claim_th` | TEXT | required |  |
| `claim_en` | TEXT |  | DERIVED translation |
| `claim_type` | TEXT | required; one of: benefit,technology,ingredient_story,test_result,free_from,usage |  |
| `source_id` | INTEGER | → data_sources.id; required |  |

## 2. Ingredients

### `ingredients` — 66 rows

🔵 CURATED knowledge — Cosmetic-science dictionary written by Bricks.  
*Production source:* R&D ingredient master

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `inci_name` | TEXT | required; unique |  |
| `common_name` | TEXT |  |  |
| `name_th` | TEXT |  |  |
| `ingredient_class` | TEXT | required | humectant, retinoid, uv_filter, vitamin_c, aha, bha, peptide, ceramide, emollient, soothing, brightener, absorbent, fragrance, preservative, solvent, other |
| `what_it_does_en` | TEXT |  |  |
| `what_it_does_th` | TEXT |  |  |
| `evidence_level` | TEXT | one of: strong,moderate,emerging,cosmetic_only |  |
| `caution_en` | TEXT |  |  |
| `is_uv_filter` | BOOLEAN | required |  |
| `uv_coverage` | TEXT |  | 'UVB', 'UVA1', 'UVA+UVB' … |
| `is_potential_irritant` | BOOLEAN | required |  |
| `is_fragrance` | BOOLEAN | required |  |

### `product_ingredients` — 4,204 rows

🟢 REAL (sourced) — Full INCI lists in label order from official product pages (retailer fallback is flagged per row).  
*Production source:* R&D formula master

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `product_id` | INTEGER | → products.id; required |  |
| `position` | INTEGER | required | order on the INCI list; 1 = highest concentration |
| `ingredient_id` | INTEGER | → ingredients.id | NULL if not yet in the dictionary |
| `inci_as_listed` | TEXT | required | verbatim from the source |
| `is_key_active` | BOOLEAN | required |  |
| `source_id` | INTEGER | → data_sources.id; required |  |
| `source_url` | TEXT |  |  |
| `shade_scope` | TEXT |  | for colour products the brand lists INCI per shade group; this is the group shown |

### `product_hero_ingredients` — 132 rows

🟢 REAL (sourced) — Hero ingredients exactly as the brand names them in official copy, linked to the dictionary.  
*Production source:* PIM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `product_id` | INTEGER | → products.id; required |  |
| `hero_name` | TEXT | required | as written in official copy, e.g. 'Salicylic Acid Sphere' |
| `ingredient_id` | INTEGER | → ingredients.id |  |
| `official_benefit_th` | TEXT |  |  |

### `ingredient_interactions` — 12 rows

🔵 CURATED knowledge — Layering rules.  
*Production source:* R&D / advisor guideline

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `class_a` | TEXT | required | ingredient_class or INCI name |
| `class_b` | TEXT | required |  |
| `interaction` | TEXT | required; one of: avoid_same_session,introduce_gradually,pairs_well,requires_sunscreen |  |
| `advice_en` | TEXT | required |  |
| `advice_th` | TEXT |  |  |

## 3. Advisor knowledge (curated)

### `skin_types` — 7 rows

🔵 CURATED knowledge — Advisor taxonomy.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT | required |  |
| `description_en` | TEXT |  |  |

### `skin_concerns` — 21 rows

🔵 CURATED knowledge — Advisor taxonomy with photo cues, Thai-FDA-safe wording and refer-out rules.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT | required |  |
| `concern_group` | TEXT | required; one of: oil_pores,acne,pigment,ageing,hydration_barrier,sensitivity,sun,makeup_goal |  |
| `photo_detectable` | TEXT | required; one of: yes,partly,no |  |
| `photo_cues_en` | TEXT |  | what the vision model should look for |
| `cosmetic_wording_th` | TEXT |  | Thai-FDA-safe way to talk about it |
| `refer_out_when_en` | TEXT |  | when to suggest seeing a dermatologist instead |

### `product_concerns` — 242 rows

🔵 CURATED knowledge — Which product serves which concern; every row states its basis (official positioning / site category / ingredient inference).  
*Production source:* Advisor guideline owned by brand + R&D

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `product_id` | INTEGER | → products.id; required |  |
| `concern_id` | INTEGER | → skin_concerns.id; required |  |
| `relevance` | INTEGER | required | 3 = primary purpose |
| `basis` | TEXT | required; one of: official_positioning,site_category,ingredient_inference |  |
| `rationale_en` | TEXT |  |  |

### `product_skin_types` — 240 rows

🔵 CURATED knowledge — Suitability by skin type, with basis.  
*Production source:* Advisor guideline

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `product_id` | INTEGER | → products.id; required |  |
| `skin_type_id` | INTEGER | → skin_types.id; required |  |
| `suitability` | TEXT | required; one of: ideal,good,caution |  |
| `basis` | TEXT | required; one of: official_positioning,ingredient_inference |  |

### `routine_steps` — 16 rows

🔵 CURATED knowledge — AM/PM step order.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `slug` | TEXT | required; unique |  |
| `name_en` | TEXT | required |  |
| `name_th` | TEXT | required |  |
| `step_order` | INTEGER | required |  |
| `applies_am` | BOOLEAN | required |  |
| `applies_pm` | BOOLEAN | required |  |
| `is_makeup` | BOOLEAN | required |  |

### `product_routine_steps` — 146 rows

🔵 CURATED knowledge — Where each product sits in a routine.  
*Production source:* Advisor guideline

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `product_id` | INTEGER | → products.id; required |  |
| `step_id` | INTEGER | → routine_steps.id; required |  |
| `use_am` | BOOLEAN | required |  |
| `use_pm` | BOOLEAN | required |  |
| `frequency_en` | TEXT |  | 'daily', '2–3 nights/week building up' |
| `usage_note_en` | TEXT |  |  |

### `range_gaps` — 5 rows

🔵 CURATED knowledge — Needs the range cannot meet + the generic external option the advisor may mention.  
*Production source:* Brand strategy

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `slug` | TEXT | required; unique |  |
| `title_en` | TEXT | required |  |
| `missing_category` | TEXT | required |  |
| `severity` | TEXT | required; one of: high,medium,low |  |
| `why_it_matters_en` | TEXT | required |  |
| `closest_in_range_product_id` | INTEGER | → products.id |  |
| `closest_in_range_limits_en` | TEXT |  |  |
| `generic_external_option_en` | TEXT | required |  |
| `generic_external_option_th` | TEXT |  |  |
| `commercial_read_en` | TEXT |  |  |

### `range_gap_concerns` — 11 rows

🔵 CURATED knowledge — Concerns that trigger each gap.

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `gap_id` | INTEGER | → range_gaps.id; required |  |
| `concern_id` | INTEGER | → skin_concerns.id; required |  |

## 4. Commerce (mock)

### `warehouses` — 3 rows

⚪ MOCK (simulated) — Fictional warehouses.  
*Production source:* WMS

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `code` | TEXT | required; unique |  |
| `name` | TEXT | required |  |
| `province` | TEXT | required |  |
| `serves` | TEXT | required | 'online_orders', 'retail_replenishment' |

### `inventory` — 1,794 rows

⚪ MOCK (simulated) — Simulated quantities, consistent with the real web stock flag.  
*Production source:* WMS / ERP

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `variant_id` | INTEGER | → product_variants.id; required |  |
| `warehouse_id` | INTEGER | → warehouses.id; required |  |
| `qty_on_hand` | INTEGER | required |  |
| `qty_reserved` | INTEGER | required |  |
| `reorder_point` | INTEGER | required |  |
| `next_restock_date` | TEXT |  |  |
| `updated_at` | TIMESTAMP | required |  |

### `inventory_movements` — 311 rows

⚪ MOCK (simulated) — Simulated stock ledger.  
*Production source:* WMS

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `variant_id` | INTEGER | → product_variants.id; required |  |
| `warehouse_id` | INTEGER | → warehouses.id; required |  |
| `movement_type` | TEXT | required; one of: receipt,sale,return,adjustment,reservation,release |  |
| `quantity` | INTEGER | required |  |
| `reference` | TEXT |  |  |
| `created_at` | TIMESTAMP | required |  |

### `customers` — 41 rows

⚪ MOCK (simulated) — Fictional people; 6 scripted demo personas.  
*Production source:* CRM / LINE OA

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `line_user_id` | TEXT | unique | LINE Messaging API userId (U + 32 hex). NULL for members who registered through the advisor: the hub owns the LINE ↔ member pairing |
| `line_display_name` | TEXT |  |  |
| `first_name` | TEXT |  |  |
| `last_name` | TEXT |  |  |
| `phone` | TEXT |  |  |
| `email` | TEXT |  |  |
| `birth_date` | TEXT |  |  |
| `gender` | TEXT | one of: female,male,non_binary,undisclosed |  |
| `preferred_language` | TEXT | required; one of: th,en |  |
| `marketing_opt_in` | BOOLEAN | required |  |
| `phone_verified` | BOOLEAN | required | TRUE once the mobile number has passed SMS OTP (the real Srichand Rewards sign-up does this) |
| `registration_channel` | TEXT | required; one of: line_rewards_form,line_oa_agent,website |  |
| `is_demo_persona` | BOOLEAN | required |  |
| `persona_brief` | TEXT |  | demo script notes for the persona |
| `created_at` | TIMESTAMP | required |  |

### `customer_addresses` — 41 rows

⚪ MOCK (simulated) — Fictional addresses (real district/postcode pairs).  
*Production source:* CRM

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `label` | TEXT | required |  |
| `recipient` | TEXT | required |  |
| `phone` | TEXT | required |  |
| `address_line` | TEXT | required |  |
| `subdistrict` | TEXT |  |  |
| `district` | TEXT |  |  |
| `province` | TEXT | required |  |
| `postal_code` | TEXT | required |  |
| `is_default` | BOOLEAN | required |  |

### `promotions` — 16 rows

🟡 DERIVED from real data — Rows with origin='observed_on_site' are real mechanics seen on srichand.com; origin='mock' rows are invented for the demo.  
*Production source:* Promotion engine

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `code` | TEXT | required; unique |  |
| `name_th` | TEXT | required |  |
| `name_en` | TEXT |  |  |
| `promo_type` | TEXT | required; one of: sale_price,bogo,gift_with_purchase,coupon,free_shipping,bundle_price,member_exclusive |  |
| `mechanics_en` | TEXT | required |  |
| `min_spend` | NUMERIC(10,2) |  |  |
| `discount_type` | TEXT | one of: percent,amount,gift,shipping |  |
| `discount_value` | NUMERIC(10,2) |  |  |
| `gift_variant_id` | INTEGER | → product_variants.id |  |
| `starts_at` | TIMESTAMP |  |  |
| `ends_at` | TIMESTAMP |  |  |
| `is_active` | BOOLEAN | required |  |
| `origin` | TEXT | required; one of: observed_on_site,mock |  |

### `promotion_variants` — 461 rows

🟡 DERIVED from real data — Variants in each promotion.  
*Production source:* Promotion engine

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `promotion_id` | INTEGER | → promotions.id; required |  |
| `variant_id` | INTEGER | → product_variants.id; required |  |

### `coupons` — 6 rows

⚪ MOCK (simulated) — Coupon code strings are placeholders.  
*Production source:* Promotion engine

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `promotion_id` | INTEGER | → promotions.id; required |  |
| `code` | TEXT | required; unique |  |
| `customer_id` | INTEGER | → customers.id | NULL = public code |
| `max_uses` | INTEGER | required |  |
| `used_count` | INTEGER | required |  |
| `expires_at` | TIMESTAMP |  |  |

### `carts` — 3 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* E-commerce platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `status` | TEXT | required; one of: open,converted,abandoned |  |
| `created_at` | TIMESTAMP | required |  |
| `updated_at` | TIMESTAMP | required |  |

### `cart_items` — 4 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* E-commerce platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `cart_id` | INTEGER | → carts.id; required |  |
| `variant_id` | INTEGER | → product_variants.id; required |  |
| `quantity` | INTEGER | required |  |
| `added_from` | TEXT | one of: advisor_recommendation,search,reorder,promotion |  |

### `orders` — 153 rows

⚪ MOCK (simulated) — Simulated orders using real SKUs, prices and the real free-shipping rule.  
*Production source:* OMS

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `order_number` | TEXT | required; unique | 'SC-260918-0042' |
| `customer_id` | INTEGER | → customers.id; required |  |
| `channel` | TEXT | required; one of: line_oa_agent,website,line_shopping |  |
| `status` | TEXT | required; one of: pending_payment,paid,packing,shipped,delivered,cancelled,refunded |  |
| `shipping_address_id` | INTEGER | → customer_addresses.id |  |
| `subtotal` | NUMERIC(10,2) | required |  |
| `discount_total` | NUMERIC(10,2) | required |  |
| `shipping_fee` | NUMERIC(10,2) | required |  |
| `points_redeemed` | INTEGER | required |  |
| `points_discount` | NUMERIC(10,2) | required |  |
| `grand_total` | NUMERIC(10,2) | required |  |
| `coupon_id` | INTEGER | → coupons.id |  |
| `points_earned` | INTEGER | required |  |
| `source_session_id` | INTEGER |  | advisor session that led to the order (attribution) |
| `placed_at` | TIMESTAMP | required |  |
| `updated_at` | TIMESTAMP | required |  |

### `order_items` — 335 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* OMS

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `order_id` | INTEGER | → orders.id; required |  |
| `variant_id` | INTEGER | → product_variants.id; required |  |
| `quantity` | INTEGER | required |  |
| `unit_price` | NUMERIC(10,2) | required |  |
| `line_total` | NUMERIC(10,2) | required |  |
| `is_gift` | BOOLEAN | required |  |

### `payments` — 153 rows

⚪ MOCK (simulated) — Simulated; methods limited to those srichand.com really offers.  
*Production source:* Payment gateway

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `order_id` | INTEGER | → orders.id; required |  |
| `method` | TEXT | required; one of: promptpay_qr,credit_card,mobile_banking,cod,truemoney,rabbit_line_pay |  |
| `status` | TEXT | required; one of: pending,succeeded,failed,expired,refunded |  |
| `amount` | NUMERIC(10,2) | required |  |
| `gateway_ref` | TEXT |  |  |
| `qr_payload_ref` | TEXT |  | mock reference to a PromptPay QR |
| `expires_at` | TIMESTAMP |  |  |
| `paid_at` | TIMESTAMP |  |  |
| `created_at` | TIMESTAMP | required |  |

### `shipments` — 146 rows

⚪ MOCK (simulated) — Simulated; carrier names real, tracking numbers fake.  
*Production source:* Carrier APIs

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `order_id` | INTEGER | → orders.id; required |  |
| `warehouse_id` | INTEGER | → warehouses.id; required |  |
| `carrier` | TEXT | required |  |
| `tracking_number` | TEXT | required; unique |  |
| `status` | TEXT | required; one of: label_created,picked_up,in_transit,out_for_delivery,delivered,failed_attempt,returned |  |
| `shipped_at` | TIMESTAMP |  |  |
| `estimated_delivery` | TEXT |  |  |
| `delivered_at` | TIMESTAMP |  |  |

### `shipment_events` — 725 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* Carrier APIs

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `shipment_id` | INTEGER | → shipments.id; required |  |
| `status` | TEXT | required |  |
| `location` | TEXT |  |  |
| `description_th` | TEXT | required |  |
| `occurred_at` | TIMESTAMP | required |  |

## 5. Loyalty — Srichand Rewards

### `loyalty_tiers` — 1 rows

🟢 REAL (sourced) — Srichand Rewards is a single-level programme: ฿25 = 1 point (official).  
*Production source:* Loyalty platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `code` | TEXT | required; unique |  |
| `name` | TEXT | required |  |
| `min_annual_spend` | NUMERIC(10,2) | required |  |
| `earn_rate_baht_per_point` | NUMERIC(10,2) |  |  |
| `benefits_en` | TEXT |  |  |
| `benefits_th` | TEXT |  |  |
| `origin` | TEXT | required; one of: official,assumed_for_demo |  |

### `loyalty_accounts` — 41 rows

⚪ MOCK (simulated) — Simulated balances.  
*Production source:* Loyalty platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `customer_id` | INTEGER | PK; → customers.id |  |
| `member_number` | TEXT | required; unique |  |
| `tier_id` | INTEGER | → loyalty_tiers.id; required |  |
| `points_balance` | INTEGER | required |  |
| `lifetime_points` | INTEGER | required |  |
| `spend_this_period` | NUMERIC(10,2) | required |  |
| `tier_expires_on` | TEXT |  |  |
| `joined_at` | TIMESTAMP | required |  |

### `loyalty_transactions` — 146 rows

⚪ MOCK (simulated) — Simulated ledger using the official earn rate.  
*Production source:* Loyalty platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `txn_type` | TEXT | required; one of: earn,redeem,bonus,expire,adjust |  |
| `points` | INTEGER | required | negative for redeem / expire |
| `order_id` | INTEGER | → orders.id |  |
| `description` | TEXT |  |  |
| `expires_on` | TEXT |  |  |
| `created_at` | TIMESTAMP | required |  |

### `rewards_catalog` — 4 rows

⚪ MOCK (simulated) — ASSUMED: Srichand publishes no redemption catalogue or point value.  
*Production source:* Loyalty platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `name_th` | TEXT | required |  |
| `name_en` | TEXT |  |  |
| `reward_type` | TEXT | required; one of: discount_coupon,free_product,free_shipping,experience |  |
| `points_cost` | INTEGER | required |  |
| `variant_id` | INTEGER | → product_variants.id |  |
| `discount_value` | NUMERIC(10,2) |  |  |
| `min_tier_id` | INTEGER | → loyalty_tiers.id |  |
| `is_active` | BOOLEAN | required |  |
| `origin` | TEXT | required; one of: official,assumed_for_demo |  |

### `reward_redemptions` — 0 rows

⚪ MOCK (simulated) — Empty.  
*Production source:* Loyalty platform

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `reward_id` | INTEGER | → rewards_catalog.id; required |  |
| `points_spent` | INTEGER | required |  |
| `coupon_id` | INTEGER | → coupons.id |  |
| `redeemed_at` | TIMESTAMP | required |  |

## 6. Advisor sessions (mock, PDPA-aware)

### `consent_records` — 94 rows

⚪ MOCK (simulated) — Simulated PDPA consent trail; consent wording is a draft for legal review.  
*Production source:* Consent management

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `consent_type` | TEXT | required; one of: rewards_terms,face_photo_analysis,skin_profile_storage,marketing,cross_border_processing |  |
| `policy_version` | TEXT | required |  |
| `consent_text_th` | TEXT | required | the exact wording the customer agreed to |
| `granted_at` | TIMESTAMP | required |  |
| `withdrawn_at` | TIMESTAMP |  |  |
| `channel` | TEXT | required |  |
| `is_minor_flow` | BOOLEAN | required | guardian consent path used |

### `customer_skin_profiles` — 7 rows

⚪ MOCK (simulated) — Simulated self-reported profiles.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `customer_id` | INTEGER | PK; → customers.id |  |
| `consent_id` | INTEGER | → consent_records.id; required |  |
| `self_reported_skin_type_id` | INTEGER | → skin_types.id |  |
| `sensitivity_level` | TEXT | one of: low,medium,high |  |
| `known_reactions` | TEXT |  |  |
| `goals` | JSON |  | ranked concern slugs |
| `current_routine` | TEXT |  |  |
| `routine_appetite` | TEXT | one of: minimal_3_steps,standard_5_steps,full_layering |  |
| `budget_per_product` | NUMERIC(10,2) |  |  |
| `wears_makeup` | TEXT | one of: never,sometimes,daily |  |
| `environment` | TEXT |  |  |
| `is_pregnant_or_nursing` | BOOLEAN |  |  |
| `updated_at` | TIMESTAMP | required |  |

### `face_analysis_sessions` — 26 rows

⚪ MOCK (simulated) — Simulated; images are never stored in the DB.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `consent_id` | INTEGER | → consent_records.id; required |  |
| `image_ref` | TEXT |  | opaque object-store key; the image itself is NEVER stored in the DB |
| `image_sha256` | TEXT |  |  |
| `image_count` | INTEGER | required |  |
| `image_expires_at` | TIMESTAMP | required | retention deadline |
| `image_deleted_at` | TIMESTAMP |  |  |
| `photo_quality` | TEXT | one of: good,usable,poor |  |
| `quality_flags` | JSON |  | ['beauty_filter_suspected','low_light','makeup_on'] |
| `model_version` | TEXT | required |  |
| `observed_skin_type_id` | INTEGER | → skin_types.id |  |
| `observed_undertone` | TEXT |  |  |
| `observed_depth` | TEXT |  |  |
| `summary_th` | TEXT |  |  |
| `status` | TEXT | required; one of: completed,rejected_quality,consent_withdrawn |  |
| `created_at` | TIMESTAMP | required |  |

### `analysis_findings` — 67 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `session_id` | INTEGER | → face_analysis_sessions.id; required |  |
| `concern_id` | INTEGER | → skin_concerns.id; required |  |
| `face_zone` | TEXT | required; one of: forehead,nose,t_zone,cheeks,under_eye,chin_jaw,lips,overall |  |
| `severity` | INTEGER | required |  |
| `confidence` | TEXT | required; one of: high,medium,low |  |
| `observation_en` | TEXT |  |  |

### `recommendations` — 24 rows

⚪ MOCK (simulated) — Simulated; product references are real.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `session_id` | INTEGER | → face_analysis_sessions.id; required |  |
| `product_id` | INTEGER | → products.id; required |  |
| `variant_id` | INTEGER | → product_variants.id | set when a shade / size was matched |
| `step_id` | INTEGER | → routine_steps.id |  |
| `rank` | INTEGER | required |  |
| `reason_en` | TEXT | required |  |
| `reason_th` | TEXT |  |  |
| `addresses_concern_id` | INTEGER | → skin_concerns.id |  |
| `outcome` | TEXT | one of: shown,added_to_cart,purchased,dismissed |  |

### `routines` — 7 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `customer_id` | INTEGER | → customers.id; required |  |
| `session_id` | INTEGER | → face_analysis_sessions.id |  |
| `name` | TEXT | required |  |
| `goal_en` | TEXT |  |  |
| `is_active` | BOOLEAN | required |  |
| `review_after` | TEXT |  | date to check in with the customer |
| `created_at` | TIMESTAMP | required |  |

### `routine_items` — 55 rows

⚪ MOCK (simulated) — Simulated.  
*Production source:* Advisor service

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `routine_id` | INTEGER | → routines.id; required |  |
| `period` | TEXT | required; one of: am,pm |  |
| `step_id` | INTEGER | → routine_steps.id; required |  |
| `product_id` | INTEGER | → products.id | NULL when the slot is a range gap |
| `gap_id` | INTEGER | → range_gaps.id |  |
| `frequency_en` | TEXT | required |  |
| `instruction_en` | TEXT |  |  |
| `instruction_th` | TEXT |  |  |

### `range_gap_events` — 147 rows

⚪ MOCK (simulated) — Simulated demand signal.  
*Production source:* Advisor service → BI

| Column | Type | Constraints | Notes |
|---|---|---|---|
| `id` | INTEGER | PK |  |
| `gap_id` | INTEGER | → range_gaps.id; required |  |
| `customer_id` | INTEGER | → customers.id |  |
| `session_id` | INTEGER | → face_analysis_sessions.id |  |
| `concern_id` | INTEGER | → skin_concerns.id |  |
| `customer_age_band` | TEXT |  |  |
| `customer_skin_type_id` | INTEGER | → skin_types.id |  |
| `generic_option_mentioned` | BOOLEAN | required |  |
| `created_at` | TIMESTAMP | required |  |

## 7. Views

| View | Purpose |
|---|---|
| `v_variant_catalog` | One row per SKU with brand, line, category, current price and web stock — what search and cart tools read |
| `v_stock_available` | On-hand minus reserved, total and for online-order warehouses |
| `v_gap_demand` | Range-gap events per gap: count, distinct customers, first/last seen — the R&D signal |
| `v_customer_360` | Customer with tier, points, order count, last order, last analysis |

## Derivation rules (the 🟡 fields)

| Field | Rule |
|---|---|
| `products` rows | 345 site listings are classified by name prefix (`[Clearance]`, `[Bogo]`, `[N ชิ้น]`, `ฟรี`, `(1 กล่อง)`, set keywords) and grouped by a normalised Thai name key; 40 hand overrides are listed at the top of `scripts/01_build_catalog.py`. Clearance lots are fuzzy-matched to canonical products by English name (≥ 0.80) or mapped by hand. |
| `products.name_en` | From the official URL slug, with hand corrections. The official site is Thai-only. |
| `products.summary_en` | Bricks' English summary of official Thai copy + INCI facts, for 33 core products. |
| `finish`, `coverage`, `oil_control_hours_claimed`, `is_water_resistant_claimed`, `tested_claims_th` | Regex over official Thai copy (e.g. `คุมมัน 14 ชั่วโมง` → 14). |
| `is_fragrance_free`, `is_alcohol_free` | From the INCI list when one exists (fact), else from the official free-from line, else NULL. |
| `product_variants.pack_format` | `sachet_box` = listing sold as a box of 6 sachets; `multipack` = `[N ชิ้น]` / twin pack / BOGO; `mini` = ≤ 20 ml/g and at most half the largest size. |
| `shade_undertone` | From official shade names/prefixes: Pinkish or `P##` → cool_pink; `N##` → neutral; `Y##` → warm_yellow; Honey/Golden → warm_golden; Peach → peach; else NULL. |
| `shade_depth_rank` | Rank of the numeric shade code within the product (105 < 110 < …). |
| `bundle_components` | Canonical product names found in the set's official name/description. Quantities only when printed in the listing name. |
| `promotions` (observed) | Sale prices, BOGO listings, ฿499 gift (verified in a live cart), September coupon tiers. Coupon code strings are placeholders. |
